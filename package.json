{
  "name": "personal_website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "hugo --minify --gc --cleanDestinationDir --enableGitInfo",
    "dev": "hugo server --gc -D"
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^8.0.0",
    "autoprefixer": "^10.4.21",
    "postcss-cli": "^11.0.0",
    "rtlcss": "^4.0.0"
  }
}
